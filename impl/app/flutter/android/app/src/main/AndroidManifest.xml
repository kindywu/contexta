<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- 本机号码读取（登录快速填充；未授予时静默返回 null 走手动输入） -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <application
        android:label="Contexta"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <!-- 全局竖屏锁定：手机端不允许切横屏。此处覆盖 Flutter 引擎启动前的
             启动窗口（闪屏）；引擎启动后的运行期旋转由 Dart 侧
             SystemChrome 兜底（lib/core/platform/app_orientation.dart）。
             见 docs/app-orientation.md -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <!-- 大屏方向限制的豁免（2026-09-18 真机实测必需）：
                 Android 16 起，targetSdk ≥ 36 的 App 在 sw ≥ 600dp 的大屏上
                 方向声明与 setRequestedOrientation 一律被系统忽略
                 （实测 ignoreOrientationRequest=true），平板因此既锁不住横屏
                 （跟随设备翻转）又被塞进竖屏窗口（两侧黑边）。
                 声明本属性后系统恢复「兼容模式」，重新尊重方向限制：
                 手机锁竖屏、平板锁横屏都能生效。
                 ⚠️ 官方标注这是**临时豁免**，targetSdk 37 起失效——
                 届时需重新评估平板方向方案。 -->
            <property
                android:name="android.window.PROPERTY_COMPAT_ALLOW_RESTRICTED_RESIZABILITY"
                android:value="true" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE"/>
        </intent>
        <package android:name="com.xiaomi.mibrain.speech"/>
        <package android:name="com.google.android.tts"/>
    </queries>
</manifest>
