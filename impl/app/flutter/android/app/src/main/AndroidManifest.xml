<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- 本机号码读取（登录快速填充；未授予时静默返回 null 走手动输入） -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <application
        android:label="Contexta"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <!-- 方向策略（2026-09-17）：手机锁竖屏、平板自适应横竖屏。
             见 docs/app-orientation.md 与 docs/adaptive-layout.md。

             **此处刻意不声明 screenOrientation**：2026-09-17 真机实测，小米
             HyperOS（Android 16 / targetSdk 36）**未启用**「大屏忽略方向声明」
             的官方行为——只要 manifest 声明了 portrait（哪怕同时声明
             resizeableActivity="true"），窗口就被 letterbox 成 666×813dp，
             永远进不了书页模式。

             手机侧竖屏改由 Dart 侧保证（lib/core/platform/app_orientation.dart：
             最短边 < 600dp → portraitUp）。代价：手机冷启动瞬间若横持，启动窗口
             可能短暂横屏（Dart 锁在引擎启动后立即生效）。 -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:resizeableActivity="true"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE"/>
        </intent>
        <package android:name="com.xiaomi.mibrain.speech"/>
        <package android:name="com.google.android.tts"/>
    </queries>
</manifest>
