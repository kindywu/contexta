package com.ak.contexta.ui.theme

import androidx.compose.animation.core.tween

// ─── Motion durations (prototype: 150/200/300ms) ───
object Motion {
    const val FastMs = 150
    const val BaseMs = 200
    const val SlowMs = 300
}
