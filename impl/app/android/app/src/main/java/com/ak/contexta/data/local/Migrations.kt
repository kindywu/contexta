package com.ak.contexta.data.local

import androidx.room.migration.Migration

/**
 * 产品尚未发布，数据库版本号为 2（通过 [fallbackToDestructiveMigration] 清空重建）。
 * 发布后在此处添加真实 migration。
 */
object Migrations {
    val ALL: Array<Migration> = emptyArray()
}
