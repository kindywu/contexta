# 复制为 .env 并填入真实值（.env 已 gitignore，绝不提交）
# DEEPSEEK_API_KEY: 必填
# JWT_SECRET: 必填，openssl rand -hex 32 生成（>=32 字节）
# ADMIN_INIT_PASSWORD: 首次启动必填（种入 admin 账号），seed 后可移除
# 其余可选，缺省见 config.rs
DEEPSEEK_API_KEY=
JWT_SECRET=
ADMIN_INIT_PASSWORD=
