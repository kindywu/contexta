services:
  contexta-server:
    image: ghcr.io/kindywu/contexta-server:latest
    container_name: contexta-server
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file:
      - ./.env
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
      - ./output:/app/output
